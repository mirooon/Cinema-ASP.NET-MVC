@model IEnumerable<Cinema.Models.MoviePositionDates>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_BackdoorLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MoviePosition.MovieTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MovieType.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MoviePosition.MovieTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MovieType.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
